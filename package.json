{
  "name": "new-folder",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build public/ts/paint.ts --outdir public/js/ --minify",
    "start": "bun run build && bun index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "dotenv": "^17.3.1",
    "ejs": "^4.0.1",
    "express": "^5.2.1"
  }
}
