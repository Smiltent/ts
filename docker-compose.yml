
services:
  db:
    image: mysql:8.0
    environment:
      - MYSQL_DATABASE=${DB_DATABASE}
      - MYSQL_USER=${DB_USER}
      - MYSQL_PASSWORD=${DB_PASSWORD}
    env_file:
      - .env
    # ports:
    #   - '3306:3306'

  ts:
    build: .
    restart: unless-stopped
    env_file:
      - .env